themedir = $(datadir)/icons/HighContrast
size = scalable
context = actions

icondir = $(themedir)/$(size)/$(context)

icon_DATA = \
	bookmark-view.svg	\
	history-view.svg

EXTRA_DIST = \
	$(icon_DATA)
